;; date

[module/date]
type = internal/date

format-background = ${colors.transparent}

label = %date%   %time%
label-font = 2

date = %a %b %d
date-alt = %A, %B %d, %Y

time = %I:%M %p
time-alt = ${self.time}
